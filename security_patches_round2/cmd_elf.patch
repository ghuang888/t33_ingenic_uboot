--- a/common/cmd_elf.c
+++ b/common/cmd_elf.c
@@ -148,7 +148,7 @@ int do_bootvx(cmd_tbl_t *cmdtp, int flag, int argc, char * const argv[])
 	unsigned long addr;		/* Address of image            */
 	unsigned long bootaddr;	/* Address to put the bootline */
 	char *bootline;			/* Text of the bootline        */
-	char build_buf[256]; /* SECURITY FIX-151: Ensure bootline fits */		/* Buffer for building the bootline */
+	char build_buf[512]; /* SECURITY FIX-151: Increased buffer for safety */		/* Buffer for building the bootline */

 	/* ---------------------------------------------------
 	 *
@@ -214,27 +214,30 @@ int do_bootvx(cmd_tbl_t *cmdtp, int flag, int argc, char * const argv[])
 			case '-':
 				if (strcmp(t, "c") == 0) {
 					if (tp != NULL)
-						sprintf(build_buf, "%s(%s)",
+						/* SECURITY FIX: Use snprintf to prevent overflow */
+						snprintf(build_buf, sizeof(build_buf), "%s(%s)",
 							argv[2], tp);
 					else
-						sprintf(build_buf, "%s", argv[2]);
+						snprintf(build_buf, sizeof(build_buf), "%s", argv[2]);
 					bootline = build_buf;
 				} else if (strcmp(t, "s") == 0) {
 					if (tp != NULL)
-						sprintf(build_buf, "%s(%s)",
+						snprintf(build_buf, sizeof(build_buf), "%s(%s)",
 							argv[2], tp);
 					else
-						sprintf(build_buf, "%s", argv[2]);
+						snprintf(build_buf, sizeof(build_buf), "%s", argv[2]);
 					bootaddr = simple_strtoul(build_buf, NULL, 16);
 				}
 				break;
 			case 's':
 				if (tp == NULL)
 					return cmd_usage(cmdtp);
-				sprintf(build_buf, "%s(%s)", argv[2], tp);
+				snprintf(build_buf, sizeof(build_buf), "%s(%s)", argv[2], tp);
 				bootline = build_buf;
 				break;
 			case 'c':
